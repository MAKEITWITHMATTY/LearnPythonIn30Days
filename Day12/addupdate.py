person = {"name": "Alex", "job": "Engineer"}

# Add new keys and values
person["age"] = 30
person["country"] = "UK"

# Update multiple at once
person.update({"city": "London", "verified": True})

print(person)
