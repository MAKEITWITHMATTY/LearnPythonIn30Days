age = 20
has_ticket = True

print(age >= 18 and has_ticket)  # True if both are True
print(age < 18 or has_ticket)    # True if either is True
print(not has_ticket)            # Flips True ↔ False
